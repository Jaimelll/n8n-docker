version: '3'
services:
  n8n:
    image: n8nio/n8n
    restart: always
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=jaduran0102@gmail.com
      - N8N_BASIC_AUTH_PASSWORD=Vanessa1
      - N8N_HOST=vgikb5mzl.localto.net
      - N8N_PORT=5678
      - N8N_PROTOCOL=https
      - GENERIC_TIMEZONE=America/Lima
      - WEBHOOK_URL=https://vgikb5mzl.localto.net
      - N8N_RUNNERS_ENABLED=true
    volumes:
      - n8n_data:/home/node/.n8n

volumes:
  n8n_data:
